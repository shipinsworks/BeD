#include "stdio.h"
#include "scenario.h"

void scenario()
{
  // c2sif_pkt_s pkt;
  printf("Hello, scenario_tc002!\n");
  // pkt.id = 1;
  // pkt.fn = 0;
  // pkt.data[0] = 0x00000001;
  // pkt.addr = 0x00000000;
  // pkt.size = 1;
  //  write( 3, 0, &pkt );
  // debug_printf("ret: %d",pkt.ret);
}
